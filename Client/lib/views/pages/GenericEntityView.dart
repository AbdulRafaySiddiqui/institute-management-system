import 'package:flutter/material.dart';

class Model {
  String title;
  DateTime dob;
  int age;
}

// var model = Model();
// model.

class GenericEntityView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
